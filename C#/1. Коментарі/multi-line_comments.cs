using System;

namespace MultiLineComments
{
    class Program
    {
        static void Main()
        {
            // Секція: Багаторядкові коментарі в C#
            // У C# багаторядкові коментарі — це текстові пояснення в коді, які позначаються символами /* і */ та можуть охоплювати кілька рядків.
            // Коментарі є частиною вихідного коду, але ігноруються компілятором і не впливають на виконання програми.
            // Використовуються для документування коду, пояснення складної логіки, тимчасового виключення великих фрагментів коду або додавання розгорнутих приміток.
            // Багаторядкові коментарі дозволяють створювати пояснення, що охоплюють кілька рядків, на відміну від однорядкових коментарів (//).
            // Розмір у пам’яті: коментарі не займають пам’ять у скомпільованій програмі, оскільки вони виключаються компілятором.

            /* Багаторядковий коментар: 
               Починається з /* і закінчується */.
               Може охоплювати кілька рядків або навіть частину рядка. */
            // Застосування: документування великих блоків коду, пояснення алгоритмів, тимчасове відключення коду.
            // Перевага: дозволяє створювати розгорнуті пояснення, що покращує читабельність складного коду.
            // Обмеження: не підходить для коротких пояснень; для документації API краще використовувати XML-коментарі (///).
            int number = 42; /* Приклад: ініціалізація змінної number зі значенням 42.
                                Цей коментар пояснює призначення змінної. */
                             /* Помилка: int x = 10; /* int y = 20; */ — вкладені коментарі не підтримуються, що призведе до помилки компіляції. */

            // Приклади використання:
            double price = 99.99; /* Встановлення ціни товару в доларах.
                                     Цей коментар може охоплювати кілька рядків
                                     для пояснення логіки. */
            /* string debug = "Test";
               int temp = 100; */ // Ці рядки закоментовані і не виконуються.
            char letter = 'A'; /* Позначення символу для подальшої обробки. */

            // Помилки:
            /* int x = 10; /* int y = 20; */ */ // Помилка: вкладені багаторядкові коментарі не підтримуються.
            /* int z = 30; // Однорядковий коментар всередині багаторядкового ігнорується. */
        }
    }
}
// Додаткові зауваження:
// 1. Синтаксис: Багаторядковий коментар починається з /* і закінчується */; усе між цими символами ігнорується компілятором.
//    Наприклад: /* int x = 5; */ — компілятор ігнорує весь код між /* і */.
// 2. Використання для дебагінгу: Багаторядкові коментарі часто використовуються для тимчасового виключення великих фрагментів коду під час тестування.
//    Наприклад: /* Console.WriteLine("Debug"); */ — відключить виведення.
// 3. Читабельність: Використовуйте багаторядкові коментарі для розгорнутих пояснень; для коротких пояснень краще використовувати однорядкові коментарі (//).
//    Наприклад: // Короткий коментар для простих пояснень.
// 4. Практичне використання: Додавайте багаторядкові коментарі для пояснення складних алгоритмів, великих блоків коду
//    або тимчасового відключення коду під час розробки чи дебагінгу.
// 5. Обмеження: Уникайте надмірного використання коментарів; вони не повинні замінювати зрозумілий код.
//    Наприклад, замість /* Збільшуємо значення на 1 */ краще використовувати зрозумілі назви змінних, наприклад, increment.
